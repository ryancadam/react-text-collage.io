import React from "react";

// import style from "./collage.module.css";
    
    // const randNum = () => Math.floor(Math.random() * (270 - 30 + 1) + 30); // remember it's (max - min + 1) + min
const collage = (props) => {
  const text1 = props.source1;

  return (
    <div>
      <h1>{line1}</h1>
    </div>
  );
};

export default collage;
